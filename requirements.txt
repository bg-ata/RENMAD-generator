streamlit>=1.35
python-pptx>=1.0
Pillow>=10.0
anthropic>=0.25
requests>=2.31
# SVG → PNG rasterisation for sponsor logos. PyMuPDF bundles its own native
# renderer in the wheel (works on Windows + Linux without GTK/Cairo).
pymupdf>=1.24
# Agenda parsing for the Title Slides tool: Word (.docx) + Excel (.xlsx)
python-docx>=1.1
openpyxl>=3.1
# Title Slides: face-aware photo cropping (Haar cascades ship with the wheel,
# no model download) + numpy for logo white-background knock-out.
opencv-python-headless>=4.8
numpy>=1.24
